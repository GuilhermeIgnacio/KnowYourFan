CREATE TABLE recommendation (
    id INTEGER PRIMARY KEY NOT NULL,
    title TEXT NOT NULL,
    link TEXT NOT NULL
);

selectAll: SELECT * FROM recommendation;

insert: INSERT INTO recommendation (title, link) VALUES (?,?);

delete: DELETE FROM recommendation WHERE id = :id;